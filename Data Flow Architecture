Sensors → ESP32 → MQTT (Wi-Fi) → Mosquitto Broker → Telegraf → InfluxDB → Grafana Dashboard
                    ↑                    ↑               ↑
                TLS Encryption    Data Processing    Alert Generation
