Sensors → ESP32 → MQTT (Wi-Fi) → Mosquitto Broker → FastAPI (backend) → SQLite → Web Dashboard (HTML, CSS, JS)
                    ↑                    ↑               ↑
                TLS Encryption    Data Processing    Alert Generation
